<UserControl x:Class="ListBoxBinding.View.BookView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
    	<Border BorderBrush="#FF464646" BorderThickness="2,5,8,2" HorizontalAlignment="Left" Height="240" 
		VerticalAlignment="Top" Width="200" Background="#FFFFE2A3" CornerRadius="10">
    		<Grid>
    			<TextBlock Height="110" TextWrapping="Wrap" Text="{Binding Title}" VerticalAlignment="Top"
				 Margin="10,20,10,0" FontSize="16"/>
    			<TextBlock TextWrapping="Wrap" Text="{Binding Author}" VerticalAlignment="Top" 
				Margin="10,140,10,0" FontSize="13.333" Foreground="#FF318512"/>
    			<TextBlock TextWrapping="Wrap" Text="{Binding Pages}" VerticalAlignment="Bottom" 
				Margin="10,0,10,15" FontSize="13.333" TextAlignment="Right" Foreground="#FF1A08D3"/>
    		</Grid>
    	</Border>
            
    </Grid>
</UserControl>
