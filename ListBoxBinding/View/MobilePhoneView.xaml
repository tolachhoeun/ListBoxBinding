<UserControl x:Class="ListBoxBinding.View.MobilePhoneView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
    	<Border BorderBrush="#FF5B5B5B" BorderThickness="10,20" HorizontalAlignment="Left" Height="240" 
		VerticalAlignment="Top" Width="160" CornerRadius="10" Background="White">
    		<StackPanel Margin="5,15,5,0">
    			<TextBlock TextWrapping="Wrap" Text="{Binding Name}" FontSize="16" FontWeight="Bold"/>
                <TextBlock TextWrapping="Wrap" Text="{Binding OEM, StringFormat=OEM: \{0\}}" FontSize="16"/>
                <TextBlock TextWrapping="Wrap" Text="{Binding OS, StringFormat=OS: \{0\}}" FontSize="16"/>
                <TextBlock TextWrapping="Wrap" Text="{Binding Year, StringFormat=Year: \{0\}}" FontSize="16"/>
    		</StackPanel>
    	</Border>
            
    </Grid>
</UserControl>
